const Joi = require('joi');
const {productSchema} = require('./product');

const cartSchema = Joi.object({
    name: Joi.string(),
    product_list: Joi.array().items(Joi.number())
});

module.exports = {
    cartSchema
};